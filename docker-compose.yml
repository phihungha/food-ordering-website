services:
  nest-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nest-server
    restart: always
    ports:
      - 3000:3000
    depends_on:
      - nest-postgres

  nest-postgres:
    image: postgres:15
    container_name: nest-postgres
    restart: always
    ports:
      - 5432:5432
    volumes:
      - nest-postgres:/var/lib/postgresql/data

volumes:
  nest-postgres:
    name: ${VOLUME_NAME:-fos-postgres-data}
